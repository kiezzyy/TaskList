{
  "name": "tasklist-workspace",
  "version": "1.0.10",
  "description": "A browser-ready workspace for kanban tasks, timers, and JSON backups.",
  "author": "TaskList",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "db:migrate": "npm run db:migrate --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:web": "concurrently -k \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev": "npm run dev:web",
    "build": "npm run build:web",
    "build:web": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend",
    "start": "npm run build:web"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.6.3"
  }
}
